"use client";
import { AppLayout } from "@/components/Layout";import { Hero } from "@/components/Hero";import { KpiCard } from "@/components/KpiCard";import { useOperation } from "@/lib/use-operation";
const money=(v:number)=>new Intl.NumberFormat("es-CO",{style:"currency",currency:"COP",maximumFractionDigits:0}).format(v||0);
export default function Page(){const {data,loading,error,load}=useOperation();const t=data?.totals||{};const sellers=data?.comerciales||[];return <AppLayout><Hero title="Comercial 360°" subtitle="Ventas históricas, metas, clientes, vendedores y almacenes de la empresa activa."/><button className="btn mb-5" onClick={load}>Actualizar</button>{error&&<div className="alert alert-red">{error}</div>}{loading?<div className="card">Consultando...</div>:data?.empty?<div className="card text-center"><h2 className="text-2xl font-black">Sin información comercial</h2></div>:<><section className="grid gap-5 md:grid-cols-4"><KpiCard title="Ventas" value={money(t.ventas)}/><KpiCard title="Clientes" value={t.clientes||0}/><KpiCard title="Facturas" value={t.facturas||0}/><KpiCard title="Ticket promedio" value={money(t.ticket_promedio)}/></section><section className="mt-6 grid gap-6 xl:grid-cols-2"><div className="card overflow-x-auto"><h2 className="text-xl font-black">Desempeño por comercial</h2><table className="table mt-4"><thead><tr><th>Comercial</th><th>Ventas</th><th>Meta</th><th>Cumplimiento</th></tr></thead><tbody>{sellers.map((r:any)=><tr key={r.nombre}><td>{r.nombre}</td><td>{money(r.venta)}</td><td>{money(r.meta)}</td><td>{Number(r.cumplimiento||0).toFixed(1)}%</td></tr>)}</tbody></table></div><div className="card overflow-x-auto"><h2 className="text-xl font-black">Top clientes</h2><table className="table mt-4"><thead><tr><th>Cliente</th><th>Ventas</th></tr></thead><tbody>{(data.top_clientes||[]).map((r:any)=><tr key={r.nombre}><td>{r.nombre}</td><td>{money(r.valor)}</td></tr>)}</tbody></table></div><div className="card overflow-x-auto"><h2 className="text-xl font-black">Ventas por almacén</h2><table className="table mt-4"><thead><tr><th>Almacén</th><th>Ventas</th></tr></thead><tbody>{(data.top_almacenes||[]).map((r:any)=><tr key={r.nombre}><td>{r.nombre}</td><td>{money(r.valor)}</td></tr>)}</tbody></table></div></section></>}</AppLayout>}
