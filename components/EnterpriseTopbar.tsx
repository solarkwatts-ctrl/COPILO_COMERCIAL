"use client";
import { useEffect,useState } from "react";
import { Bell,Building2,CalendarDays,Clock3,Moon,Search,Sun } from "lucide-react";
import { supabaseBrowser } from "@/lib/supabase/client";
export function EnterpriseTopbar(){const [date,setDate]=useState("");const [time,setTime]=useState("");const [company,setCompany]=useState("");const [user,setUser]=useState<any>(null);const [dark,setDark]=useState(false);useEffect(()=>{try{const u=JSON.parse(localStorage.getItem("copiloto_user")||"null");setUser(u);const id=localStorage.getItem("active_company_id")||u?.empresa_id;if(id)supabaseBrowser.from("empresa").select("nombre,nombre_comercial").eq("id",id).single().then(({data})=>setCompany(data?.nombre_comercial||data?.nombre||""));}catch{}const tick=()=>{const n=new Date();setDate(n.toLocaleDateString("es-CO",{weekday:"long",day:"2-digit",month:"long",year:"numeric",timeZone:"America/Bogota"}));setTime(n.toLocaleTimeString("es-CO",{hour:"2-digit",minute:"2-digit",timeZone:"America/Bogota"}))};tick();const t=setInterval(tick,60000);return()=>clearInterval(t)},[]);function toggle(){const next=!dark;setDark(next);document.documentElement.classList.toggle("dark",next)}return <header className="enterprise-topbar"><div className="min-w-0"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.18em] text-slate-400"><Building2 size={14}/>{company||"Empresa activa"}</div><div className="mt-1 flex flex-wrap items-center gap-x-3 gap-y-1"><p className="font-black text-slate-950 dark:text-white">Hola, {user?.nombre?.split(" ")[0]||"Usuario"}</p><span className="hidden text-slate-300 sm:inline">•</span><p className="capitalize text-sm text-slate-500"><CalendarDays size={14} className="inline mr-1"/>{date}</p><p className="text-sm text-slate-500"><Clock3 size={14} className="inline mr-1"/>{time}</p></div></div><div className="flex items-center gap-2"><div className="hidden xl:flex items-center gap-2 rounded-2xl border bg-slate-50 px-3 py-2 text-sm text-slate-500"><Search size={16}/><span>Buscar en la empresa…</span></div><button className="icon-btn" aria-label="Notificaciones"><Bell size={18}/><span className="notification-dot"/></button><button className="icon-btn" onClick={toggle} aria-label="Cambiar tema">{dark?<Sun size={18}/>:<Moon size={18}/>}</button><div className="hidden sm:flex h-10 w-10 items-center justify-center rounded-2xl bg-slate-950 font-black text-white">{(user?.nombre||"U").slice(0,1)}</div></div></header>}
